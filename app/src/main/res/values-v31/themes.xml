<resources>

    <!-- Splash theme for 31+ API levels -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="android:windowLightStatusBar">true</item>
        <item name="windowSplashScreenBackground">#ffffffff</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/vector_turtlpass_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.App.NoActionBar</item>
    </style>

</resources>
